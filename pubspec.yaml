name: exam_quiz
description: A simple quiz app in French.
distribution:
  - name: exam_quiz
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
